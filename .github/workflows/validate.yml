name: "Validate devcontainer-feature.json files"
on:
  workflow_dispatch:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Validate devcontainer-feature.json files"
        uses: devcontainers/action@v1
        with:
          validate-only: "true"
          base-path-to-features: "./src"

      - name: "Test Features"
        uses: devcontainers/action@v1
        with:
          features-test-all: "true"
          base-path-to-features: "./src"
          base-path-to-tests: "./test"
